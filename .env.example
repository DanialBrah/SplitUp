DATABASE_URL="mysql://root:@localhost:3306/splitup"

# Auth.js (next-auth) session secret — generate your own, e.g. `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`
AUTH_SECRET="replace-with-a-random-32+-byte-secret"